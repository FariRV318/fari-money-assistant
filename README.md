# Fari Money Assistant v14 — Direct Open

- Login gate removed completely.
- App opens straight to the Dashboard.
- Local browser data remains available and auto-saves.
- Supabase/account tools can be revisited later, but they do not block app entry.
- Old service worker is disabled to reduce stale GitHub Pages builds.

Upload every file in this ZIP directly to the root of the `fari-money-assistant` GitHub repository.
